// Smoke test placeholder. Real widget tests should mount specific screens
// rather than the full WageWiseApp (which requires Supabase + dotenv init).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
